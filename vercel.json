{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm --prefix client install --legacy-peer-deps && npm run build && npm --prefix client run build && npm --prefix server install --legacy-peer-deps",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
