{
  "installCommand": "npm install",
  "buildCommand": "npm --prefix client install && npm run build && npm --prefix client run build && npm --prefix server install",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
